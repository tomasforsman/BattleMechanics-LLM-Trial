function choose_action(self)
    -- Simple AI: prefer healing allies if they're hurt, otherwise attack
    print(self.name .. " (AI) is deciding what to do...")
    
    -- This is a basic AI script that would need integration with the C# system
    -- The actual implementation would need to return action/target choices
    -- to the C# combat manager
    
    return nil  -- Fall back to C# simple AI
end
